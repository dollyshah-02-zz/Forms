import { EmployeePhotoDirective } from './employee-photo.directive';

describe('EmployeePhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeePhotoDirective();
    expect(directive).toBeTruthy();
  });
});
